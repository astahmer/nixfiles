# Assets agent instructions

- When proposing work, requesting feedback, or showcasing changes, use indexed lists so replies can be answered unambiguously.
- For one section, number items `1`, `2`, `3`; for multiple sections, prefix each item with its section letter, such as `A1`, `A2`, `B1`.
- Keep asset fixtures value-free: never add real secrets, tokens, private keys, or session material.
