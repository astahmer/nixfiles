# Codex config template managed by nixfiles.
# This template provides the native Codex defaults for a fresh machine.
# Existing app settings remain user-owned after initialization.

model = "codex-perso/gpt-6-luna"
model_catalog_json = "HOME_DIR/.codex/opencodex-catalog.json"
model_reasoning_effort = "high"
service_tier = "fast"
personality = "pragmatic"
autoupdate = false

[features]
hooks = true
js_repl = false
memories = true
fast_mode = true

[desktop]
followUpQueueMode = "queue"
localeOverride = "en-US"
enabled-reasoning-efforts = [ "low", "medium", "high", "xhigh", "ultra", "max" ]
preventSleepWhileRunning = true
notifications-turn-mode = "always"
appearanceTheme = "system"

[memories]
generate_memories = true
use_memories = true

[apps.connector_openai_hotline]
enabled = false

[apps.connector_openai_safety_settings]
enabled = false

# Computer use (screen + browser control). The Codex app defaults this to
# disabled; enabled so the unified-computer-use plugin's cua_repl attaches on
# a fresh machine without a manual flip in Settings.
[mcp_servers.computer-use]
command = "./Codex Computer Use.app/Contents/SharedSupport/SkyComputerUseClient.app/Contents/MacOS/SkyComputerUseClient"
args = [ "mcp" ]
cwd = "."
enabled = true
