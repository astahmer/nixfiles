id: require-explicit-schema-declare-type-guard
valid:
  - "const value = Schema.declare<string>((input): input is string => typeof input === 'string');"
  - "const values = Schema.declare<Array<string>>(Array.isArray);"
invalid:
  - "const value = Schema.declare<string>((input) => typeof input === 'string');"
