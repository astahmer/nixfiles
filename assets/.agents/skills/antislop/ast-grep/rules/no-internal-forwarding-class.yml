id: no-internal-forwarding-class
language: TypeScript
message: Implementation classes must not forward a static domain surface; use the owning domain directly.
severity: error
rule:
  pattern: class $CLASS { $$$BEFORE static readonly $FIELD = $TARGET.$MEMBER $$$AFTER }
