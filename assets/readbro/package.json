{
  "name": "readbro",
  "version": "0.3.0",
  "description": "IR-aware read cache MCP + CLI for coding agents",
  "type": "module",
  "packageManager": "pnpm@11.18.0",
  "bin": {
    "readbro": "./src/main.ts"
  },
  "dependencies": {
    "@effect/ai": "0.37.0",
    "@effect/cli": "0.77.0",
    "@effect/platform-node": "0.108.1",
    "effect": "3.22.1"
  },
  "scripts": {
    "build": "bun build ./src/main.ts --compile --minify --bytecode --outfile readbro",
    "benchmark": "node --experimental-transform-types --experimental-strip-types benchmark/run.mjs",
    "test": "./run-tests",
    "test:node": "node --experimental-transform-types --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "26.1.2",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=24"
  }
}
