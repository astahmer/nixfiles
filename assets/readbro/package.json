{
  "name": "readbro",
  "version": "0.2.0",
  "description": "IR-aware read cache MCP for coding agents (composto compression + session diffs)",
  "type": "module",
  "bin": {
    "readbro": "src/serve.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "scripts": {
    "benchmark": "node benchmark/run.mjs",
    "test": "node --test test/smoke.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
