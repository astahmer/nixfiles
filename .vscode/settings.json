{
    "git.enabled": false,
    // ── Nix IDE + nixd language server ──────────────────────────
    "nix.serverSettings": {
        "nixd": {
            "nixpkgs": {
                "expr": "import (builtins.getFlake \"${workspaceFolder}\").inputs.nixpkgs { }"
            },
            "options": {
                "nixos": {
                    "expr": "(builtins.getFlake \"${workspaceFolder}\").nixosConfigurations.workstation.options"
                },
                "home-manager": {
                    "expr": "(builtins.getFlake \"${workspaceFolder}\").homeConfigurations.macbook.options"
                }
            }
        }
    }
}
