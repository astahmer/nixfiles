# Reference repositories

Local clones under `.references/` (gitignored). Refresh with the add-reference-repository skill.

| Name | URL | Path | Why |
| --- | --- | --- | --- |
| composto | https://github.com/mertcanaltin/composto | `.references/composto` | Causal memory + IR compression for coding agents; hooks/MCP/skills source of truth |
