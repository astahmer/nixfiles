--- a/src/codex/main-account.ts
+++ b/src/codex/main-account.ts
@@ -447,5 +447,9 @@ export function getMainAccountToken(): { accessToken: string; chatgptAccountId: string } | null {
 export function getMainAccountToken(): { accessToken: string; chatgptAccountId: string } | null {
   const tokens = readCodexTokens();
   if (!tokens?.access_token) return null;
-  return { accessToken: tokens.access_token, chatgptAccountId: tokens.account_id };
+  // Keep freshness checks on the same JWT-derived identity used by token validation.
+  return {
+    accessToken: tokens.access_token,
+    chatgptAccountId: extractAccountId(tokens.id_token, tokens.access_token) ?? tokens.account_id,
+  };
 }
